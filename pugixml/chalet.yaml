name: oflibs
version: "1.0"
externalDependencies:
  pugixml:
    repository: https://github.com/zeux/pugixml
    # tag: v1.14
    commit: "27b68329de32"
    kind: git

targets:
  pugixml:
    kind: cmakeProject
    location: ${external:${name}}
    recheck: false
    rebuild: false
    install: true
    defines:
      - CMAKE_BUILD_TYPE=Release
      - CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded
      # - CMAKE_ARCHIVE_OUTPUT_DIRECTORY=lib
      # - PUGIXML_INSTALL=ON
      # - PUGIXML_INSTALL=OFF
      - PUGIXML_BUILD_TESTS=OFF
      - BUILD_SHARED_LIBS=OFF
      - CMAKE_C_STANDARD=20
      - CMAKE_CXX_STANDARD=20

    # Mirrors apothecary's Android CMake invocation. The reusable workflow
    # supplies the ABI and NDK root for each matrix entry.
    defines[env:ANDROID_ABI]:
      - CMAKE_TOOLCHAIN_FILE=${env:ANDROID_NDK_ROOT}/build/cmake/android.toolchain.cmake
      - CMAKE_ANDROID_ARCH_ABI=${env:ANDROID_ABI}
      - ANDROID_ABI=${env:ANDROID_ABI}
      - ANDROID_API=${env:ANDROID_API}
      - ANDROID_PLATFORM=${env:ANDROID_PLATFORM}
      - ANDROID_TOOLCHAIN=clang
      - ANDROID_NDK=${env:ANDROID_NDK_ROOT}
      - ANDROID_NDK_ROOT=${env:ANDROID_NDK_ROOT}
      - ANDROID_STL=c++_static

    defines[platform:macos]:
      - CMAKE_OSX_ARCHITECTURES=x86_64;arm64
      - CMAKE_OSX_DEPLOYMENT_TARGET=11.0

distribution:
  pugixml:
    kind: archive
    format: zip
    include:
      - ${external:${name}}/LICENSE**
      - ${externalBuild:${name}}/install/include
      - ${externalBuild:${name}}/install/lib
      # - ${externalBuild:${name}}/${ar:${name}}
