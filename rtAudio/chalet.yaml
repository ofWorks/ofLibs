name: oflibs
version: "1.0"
externalDependencies:
  rtAudio:
    repository: https://github.com/thestk/rtaudio
    # tag: 6.0.1
    commit: 409636b
    kind: git
targets:
  rtAudio:
    kind: cmakeProject
    location: ${external:${name}}
    recheck: false
    rebuild: false
    install: true
    defines:
      - BUILD_SHARED_LIBS=OFF
      - CMAKE_BUILD_TYPE=Release
      - CMAKE_POSITION_INDEPENDENT_CODE=ON
      - CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded

      # - CMAKE_INCLUDE_OUTPUT_DIRECTORY=include
      - CMAKE_INSTALL_PREFIX=install
      # - CMAKE_INSTALL_LIBDIR=lib
      - CMAKE_INSTALL_LIBDIR=lib
    defines[platform:macos]:
      - CMAKE_OSX_ARCHITECTURES=x86_64;arm64
      - CMAKE_OSX_DEPLOYMENT_TARGET=10.15
    defines[:web]:
      - RTAUDIO_API_ALSA=OFF
      - RTAUDIO_API_PULSE=OFF
      - RTAUDIO_API_OSS=OFF
      - RTAUDIO_API_JACK=OFF
      - RTAUDIO_API_CORE=OFF
      - RTAUDIO_API_WASAPI=OFF
      - RTAUDIO_API_ASIO=OFF
      - RTAUDIO_API_DS=OFF
    # defines[toolchain:aarch64-linux-gnu-gcc]:
    # defines[toolchain:{aarch64-linux-gnu-gcc,arm-linux-gnueabihf-gcc}]:
    # - CMAKE_SYSROOT=${cwd}/raspbian
    # - CMAKE_LIBRARY_PATH=${cwd}/raspbian/usr/lib
    # - CMAKE_INCLUDE_PATH=${cwd}/raspbian/usr/include

distribution:
  rtAudio:
    kind: archive
    format: zip
    include:
      - ${external:${name}}/LICENSE**
      # - ${external:${name}}/include
      # - ${externalBuild:${name}}/gen_include
      # - ${external:${name}}/include
      # - ${externalBuild:${name}}/include
      - ${externalBuild:${name}}/install/include
      - ${externalBuild:${name}}/install/lib
      # - ${externalBuild:${name}}/${ar:usb}
      # - ${externalBuild:${name}}/${ar:${name}}
      # - ${externalBuild:${name}}/*.a
      # - ${externalBuild:${name}}/*.lib
      # - ${externalBuild:${name}}/*.pc
