name: ofLibs glew
version: "2.3.1"
externalDependencies:
  glew:
    repository: https://github.com/Perlmint/glew-cmake
    commit: 65091a8
    kind: git
targets:
  glew:
    kind: cmakeProject
    location: ${external:${name}}
    recheck: false
    rebuild: false
    install: true
    defines:
      # - CMAKE_ARCHIVE_OUTPUT_DIRECTORY=lib
      - glew-cmake_BUILD_SHARED=OFF
      - ONLY_LIBS=ON
      - CMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded
      - CMAKE_BUILD_TYPE=Release
      - CMAKE_EXPORT_COMPILE_COMMANDS=ON
      - GLEW_BUILD_STATIC=ON

      # - CMAKE_ARCHIVE_OUTPUT_DIRECTORY=lib
    defines[:linux]:
      - GLEW_EGL=ON # Use EGL instead of GLX
    defines[platform:macos]:
      - CMAKE_OSX_DEPLOYMENT_TARGET=11.6
      - CMAKE_OSX_ARCHITECTURES=x86_64;arm64
    # FIXME: remove - only a test to see if it works as a parameter
    # defines[toolchain:aarch64-linux-gnu-gcc]:
    # defines[toolchain:{aarch64-linux-gnu-gcc,arm-linux-gnueabihf-gcc}]:
    #   - CMAKE_SYSROOT=${cwd}/raspbian
    #   - CMAKE_LIBRARY_PATH=${cwd}/raspbian/usr/lib
    #   - CMAKE_INCLUDE_PATH=${cwd}/raspbian/usr/include
    # defines[toolchain:arm-linux-gnueabihf-gcc]:
    #   - CMAKE_SYSROOT=${cwd}/raspbian
    #   - CMAKE_LIBRARY_PATH=${cwd}/raspbian/usr/lib
    #   - CMAKE_INCLUDE_PATH=${cwd}/raspbian/usr/include

distribution:
  glew:
    kind: archive
    format: zip
    include:
      - ${external:${name}}/LICENSE**
      # - ${external:${name}}/include
      - ${externalBuild:${name}}/install/include
      - ${externalBuild:${name}}/install/lib
      # - ${externalBuild:${name}}/${ar:${name}}

    # kind: archive
    # format: zip
    # include:
    #   - ${external:${name}}/libusb/libusb/*.h
    # include[:macos]:
    #   - ${externalBuild:${name}}**.a
    # include[:windows]:
    #   - ${externalBuild:${name}}**.lib

    # - ${externalBuild:${name}}${ar:libusb}
    # - ${externalBuild:${name}}/${so:libusb}
    # - ${so:${name}}
