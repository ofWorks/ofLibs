name: utfcpp

on:
  push:
    paths:
      - .github/workflows/utfcpp.yml
      - utfcpp/**

jobs:
  ci:
    permissions:
      contents: write

    uses: ./.github/workflows/_build.yml
    with: { lib: utfcpp, version: v1.0 }
    secrets: inherit
  android:
    permissions:
      contents: write
    uses: ./.github/workflows/_build_android.yml
    with: { lib: utfcpp, version: v1.0 }
    secrets: inherit
# bump 2026-07-24: rebuild on windows-2022 (MSVC 14.4x) runner pin
